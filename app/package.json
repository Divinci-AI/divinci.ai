{
  "name": "divinci-app-funnel",
  "private": true,
  "description": "divinci.app funnel — deploy via these scripts so the pinned-asset guard always runs",
  "scripts": {
    "check": "node check-pinned-assets.mjs",
    "check:live": "node check-pinned-assets.mjs --live",
    "deploy": "node check-pinned-assets.mjs && env -u CLOUDFLARE_API_TOKEN wrangler deploy --env='' && node check-pinned-assets.mjs --live",
    "deploy:staging": "node check-pinned-assets.mjs && env -u CLOUDFLARE_API_TOKEN wrangler deploy --env staging"
  }
}
