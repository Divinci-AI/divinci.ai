+++
title = "The Open Web Vector Initiative"
description = "A public, per-site retrieval index for the open web: every site gets its own vector database, its own embeddings, and a chat endpoint grounded in its own words — with the permission check running before the crawler, not after."
template = "open-web-vectors.html"
+++
